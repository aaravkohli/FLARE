# Legacy result status

Result files without a matching, hash-valid `.provenance.json` sidecar are retained only as historical working artifacts. They are not current evidence and must not be used for README metrics, completion claims, or comparisons until the corresponding experiment is rerun under an explicit protocol and promoted.
